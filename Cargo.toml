[package]
name = "parsething"
version = "0.1.0"
edition = "2024"

[dependencies]
packed_struct = { version = "0.10.1", features = ["serde"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
